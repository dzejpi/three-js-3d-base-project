import { GraphicsSetting, GraphicsValue } from './graphicsTypes';

export const graphicsDefaults: Record<GraphicsSetting, GraphicsValue> = {
	resolutionScale: 1,
	fov: 90,
	maxFps: 120,
	vsync: true,
};
