import { GameplaySetting, GameplayValue } from './gameplayTypes';

export const gameplayDefaults: Record<GameplaySetting, GameplayValue> = {
	mouseSensitivity: 1,
	invertY: false,
	toggleSprint: true,
};
